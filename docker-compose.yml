services:
    app:
        container_name: data-distributor-notification-app
        build: .
        ports:
            - "8080:8080"
        depends_on:
            - db
        volumes:
            - ./migrations:/app/migrations

    # db:
    #     container_name: data-distributor-notification-db
    #     image: postgres:13-alpine
    #     ports:
    #         - "5432:5432"
    #     env_file:
    #         - ./.env
    #     volumes:
    #         - pgdata:/var/lib/postgresql/data

volumes:
    pgdata: {}
